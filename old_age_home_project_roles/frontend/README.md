# Frontend - Old Age Home (React)

Install:
```
cd frontend
npm install
npm start
```

This is a simple React app using plain CSS and axios. It expects the backend API available at `/api/*` (use a proxy or run backend on same domain).
